import 'package:flutter/material.dart';

class ThemeColor {
  final Color bgColor;
  final Color textColor;
  final IconData themeIcon;

  ThemeColor({this.bgColor, this.textColor, this.themeIcon});
}